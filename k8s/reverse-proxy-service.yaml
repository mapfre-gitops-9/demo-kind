apiVersion: v1
kind: Service
metadata:
  name: reverse-proxy-service
  labels:
    app: reverse-proxy
spec:
  type: NodePort
  selector:
    app: reverse-proxy
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
